spring:
  application:
    name: BookingSystem

  datasource:
    url: jdbc:postgresql://localhost:5432/booking_system_db
    username: postgres
    password: Manudeep@050607
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  port: 8080